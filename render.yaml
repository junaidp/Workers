services:
  - type: web
    name: workers-marketplace-client
    env: static
    rootDirectory: client
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    envVars:
      - key: NODE_ENV
        value: production
      - key: VITE_API_URL
        value: https://workers-marketplace-server.onrender.com
